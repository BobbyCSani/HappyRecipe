<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".feature.base.HappyRecipeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HappyRecipe"
        tools:targetApi="31" >
        <activity android:name="com.happy.recipe.feature.base.HomeActivity" />
        <activity android:name="com.happy.recipe.feature.search.SearchActivity" />
        <activity android:name="com.happy.recipe.feature.recipe.RecipeDetailActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="spoonacular.com" />
            </intent-filter>
        </activity>
        <activity android:name="com.happy.recipe.feature.base.SplashScreenActivity"
            android:noHistory="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.happy.recipe.feature.recipe.ListRecipeActivity" />
        <activity android:name="com.happy.recipe.feature.base.WebViewActivity" />
    </application>

</manifest>